services:
  bot:
    build: .
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_CHAT_IDS=${ADMIN_CHAT_IDS}
      - REVIEW_CHAT_ID=${REVIEW_CHAT_ID}
    volumes:
      - bot-data:/app/data
volumes:
  bot-data:
